"""
The `krfd` package consists of two subpackages. The `krfd.cli` and `krfd.lib`
packages. The `krfd.cli` package contains modules to configure each component
of the CLI. The `krfd.lib` package contains packages defining the scheduler,
executor, triager, and helper functions for the aformentioned.
"""
import krfd.cli
import krfd.lib
